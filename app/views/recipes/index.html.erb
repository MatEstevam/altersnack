<div class="container mt-2">
  <h1>My Recipes</h1>

  <ul>
    <% @recipes.each do |recipe| %>
      <li class="m-2"><%= link_to recipe.name.capitalize, recipe_path(recipe) %></li>
  <% end %>
  </ul>
  <%= link_to "New Recipe", new_recipe_path, class: 'btn btn-primary' %>

</div>
